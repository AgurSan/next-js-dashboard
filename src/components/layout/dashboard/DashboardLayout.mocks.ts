import { IDashboardLayout } from './DashboardLayout';

const base: IDashboardLayout = {
  children: 'This is a mock child content for the dashboard layout',
};

export const mockDashboardLayoutProps = {
  base,
};
